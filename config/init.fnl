;; scion sample config — becomes live at milestone 5 (Fennel scripting).
;; Until then this file documents the intended shape: load the default
;; plugins, select the vim keymap, and demonstrate redefinition.
;;
;; (use :fs-host)
;; (use :vim)
;; (use :pick)
;; (set-default-keymap :vim)
